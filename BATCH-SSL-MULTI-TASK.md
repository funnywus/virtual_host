# 批量SSL证书 - 多任务并行功能

## 功能概述

批量SSL证书功能现已支持**多任务并行**和**Tab切换**，可以同时运行多个批量获取任务，并在不同任务之间自由切换查看进度。

## 主要特性

### 1. 多任务并行 ✓
- 支持同时运行多个批量SSL证书获取任务
- 每个任务独立执行，互不干扰
- 自动轮询所有运行中的任务状态

### 2. Tab切换查看 ✓
- 使用Tab标签页管理所有任务
- 每个Tab显示一个任务的详细信息
- Tab标签显示任务状态图标和进度徽章
- 可关闭已完成的任务Tab

### 3. 任务状态管理 ✓
- **运行中/等待中**：显示加载动画图标
- **已完成**：显示绿色对勾图标
- **部分失败/异常**：显示红色叉号图标
- 实时更新任务进度和日志

### 4. 智能轮询 ✓
- 自动轮询所有运行中的任务
- 没有运行中的任务时自动停止轮询
- 节省系统资源

## 使用方法

### 创建新任务

1. 点击"批量获取证书"按钮
2. 如果是首次使用，会显示新建任务页面
3. 选择证书类型（Let's Encrypt 或 ZeroSSL）
4. 点击"开始批量获取"按钮
5. 任务创建成功后自动切换到任务Tab

### 查看任务进度

- **Tab标签**：显示任务创建时间和进度徽章
- **进度条**：显示整体完成百分比
- **统计信息**：总数、完成、成功、失败
- **日志输出**：实时显示详细的执行日志

### 切换任务

- 点击不同的Tab标签切换查看不同任务
- 运行中的任务会自动更新进度
- 已完成的任务可以查看历史日志

### 创建多个任务

1. 在任务列表页面，点击"新建任务"按钮
2. 返回新建任务页面
3. 重新选择域名和证书类型
4. 点击"开始批量获取"创建新任务
5. 新任务会添加到Tab列表中

### 关闭任务

- 只有已完成、部分失败或异常的任务可以关闭
- 点击Tab标签上的关闭按钮（×）
- 运行中的任务不能关闭，防止误操作

### 刷新任务

- 点击"刷新"按钮手动刷新当前任务状态
- 适用于网络不稳定或需要立即查看最新状态的情况

## 界面说明

### 新建任务页面
```
┌─────────────────────────────────────┐
│ 处理范围: 10 个域名 (已勾选)        │
│                                     │
│ 证书类型: [Let's Encrypt ▼]        │
│                                     │
│      [开始批量获取]                 │
└─────────────────────────────────────┘
```

### 任务列表页面
```
┌─────────────────────────────────────┐
│ [任务 14:30:25 ⚙️ 5/10] [任务 14:35:12 ✓] │
│ ─────────────────────────────────── │
│ 任务ID: 1234567890-abcdef           │
│ 状态: 执行中                        │
│                                     │
│ ████████████░░░░░░░░ 50%           │
│                                     │
│ 总数 10 | 完成 5 | 成功 4 | 失败 1  │
│                                     │
│ ┌─────────────────────────────┐   │
│ │ [日志输出区域]               │   │
│ │ [2024-01-01 14:30:25] ...   │   │
│ └─────────────────────────────┘   │
│                                     │
│ [新建任务]              [关闭][刷新] │
└─────────────────────────────────────┘
```

## 技术实现

### 数据结构

```javascript
// 任务对象
{
  job_id: 'unique-job-id',
  status: 'running',  // pending, running, completed, completed_with_errors, error
  total: 10,
  done: 5,
  success: 4,
  failed: 1,
  log: '...',
  results: [...],
  created_at: '2024-01-01 14:30:25',
  updated_at: '2024-01-01 14:30:30'
}
```

### 状态管理

```javascript
// 所有任务列表
const batchSslTasks = ref([])

// 当前激活的任务Tab
const activeTaskTab = ref('')

// 当前显示的任务
const currentBatchTask = computed(() => {
  return batchSslTasks.value.find(t => t.job_id === activeTaskTab.value)
})

// 是否有正在运行的任务
const hasRunningTask = computed(() => {
  return batchSslTasks.value.some(t => 
    t.status === 'running' || t.status === 'pending'
  )
})
```

### 轮询机制

```javascript
async function loadBatchSslJob() {
  // 轮询所有运行中的任务
  const runningTasks = batchSslTasks.value.filter(
    t => t.status === 'running' || t.status === 'pending'
  )
  
  for (const task of runningTasks) {
    // 更新每个任务的状态
    const res = await api.get(`/ssl/batch-issue/${task.job_id}`)
    Object.assign(task, res)
  }
  
  // 如果没有运行中的任务，停止轮询
  if (runningTasks.length === 0) {
    stopBatchSslPolling()
  }
}
```

## 使用场景

### 场景1：大批量域名分批处理
- 第一批：10个域名，使用 Let's Encrypt
- 第二批：15个域名，使用 ZeroSSL
- 第三批：8个域名，重试失败的域名

可以同时创建3个任务，分别处理不同批次的域名。

### 场景2：不同证书类型对比
- 任务A：使用 Let's Encrypt 获取证书
- 任务B：使用 ZeroSSL 获取证书

同时运行两个任务，对比不同证书提供商的速度和成功率。

### 场景3：持续监控
- 创建任务后可以关闭对话框
- 稍后重新打开查看进度
- 任务会在后台持续执行

## 注意事项

### 1. 任务过期
- 任务在24小时后会自动清理
- 服务器重启会导致任务丢失
- 建议及时查看任务结果

### 2. 并发限制
- 虽然支持多任务，但建议不要同时运行过多任务
- 每个任务都会占用服务器资源
- 建议同时运行不超过3个任务

### 3. 网络稳定性
- 批量任务需要稳定的网络连接
- 如果网络不稳定，可能导致任务失败
- 可以使用刷新按钮手动更新状态

### 4. 任务管理
- 及时关闭已完成的任务，保持界面整洁
- 运行中的任务不能关闭
- 可以创建新任务而不影响现有任务

## 相关文件

- `frontend/src/views/Domains.vue` - 域名管理页面，包含多任务功能
- `backend/routes/ssl.js` - SSL证书路由，后端任务管理
- `BATCH-SSL-TASK-EXPIRY.md` - 任务过期问题说明

## 未来改进

### 计划中的功能
1. **任务持久化** - 将任务信息保存到数据库
2. **任务历史** - 查看所有历史任务记录
3. **任务导出** - 导出任务结果为CSV或JSON
4. **任务通知** - 任务完成后发送通知
5. **任务优先级** - 设置任务执行优先级
6. **任务暂停/恢复** - 支持暂停和恢复任务

## 常见问题

### Q: 可以同时运行多少个任务？
A: 理论上没有限制，但建议不超过3个任务同时运行，以避免服务器资源不足。

### Q: 关闭对话框后任务会停止吗？
A: 不会。任务在后台持续执行，重新打开对话框可以继续查看进度。

### Q: 如何查看已完成的任务？
A: 已完成的任务会保留在Tab列表中，可以随时切换查看。如果不需要，可以点击关闭按钮删除。

### Q: 任务失败了怎么办？
A: 可以查看任务日志了解失败原因，然后创建新任务重试失败的域名。

### Q: 为什么有些任务不能关闭？
A: 运行中的任务不能关闭，防止误操作。只有已完成、部分失败或异常的任务可以关闭。
