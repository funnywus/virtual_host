# 分片上传功能测试指南

## 测试前准备

1. 确保后端服务运行在端口 6002
2. 确保前端开发服务器正常运行
3. 准备测试文件：
   - 小文件 (< 5MB): 例如 2MB 的图片
   - 大文件 (> 5MB): 例如 10MB 的视频或压缩包

## 测试步骤

### 1. 小文件上传测试（普通上传）

**目的**: 验证小于 5MB 的文件使用普通上传

**步骤**:
1. 登录用户上传页面（输入授权码）
2. 点击"上传文件"按钮
3. 选择一个 < 5MB 的文件（例如 2MB 图片）
4. 点击"开始上传"

**预期结果**:
- 进度条显示 0-50%: 上传进度
- 进度条显示 50-100%: 显示"处理中..."
- 上传完成后显示"完成"标签（绿色）
- 文件列表中出现新上传的文件

### 2. 大文件上传测试（分片上传）

**目的**: 验证大于 5MB 的文件使用分片上传

**步骤**:
1. 在上传对话框中选择一个 > 5MB 的文件（例如 10MB 视频）
2. 点击"开始上传"

**预期结果**:
- 进度条显示 0-90%: 显示百分比（例如 "45%"）
- 进度条显示 90-100%: 显示"合并中..."
- 上传完成后显示"完成"标签（绿色）
- 文件列表中出现新上传的文件

**观察要点**:
- 进度条应该平滑增长（不会卡住）
- 状态标签应该从"等待" → "上传百分比" → "合并中..." → "完成"

### 3. 取消上传测试

**目的**: 验证可以取消正在上传的大文件

**步骤**:
1. 选择一个 > 5MB 的文件开始上传
2. 在上传过程中（进度 < 100%）点击文件右侧的"×"按钮

**预期结果**:
- 显示提示"已取消上传"
- 文件状态变为"失败"（红色标签）
- 后端临时文件被清理

### 4. 多文件并发上传测试

**目的**: 验证可以同时上传多个文件

**步骤**:
1. 选择多个文件（包括大小文件混合）
2. 点击"开始上传"

**预期结果**:
- 所有文件按顺序开始上传
- 每个文件独立显示进度
- 大文件使用分片上传，小文件使用普通上传
- 所有文件最终都显示"完成"或"失败"

### 5. 文件夹上传测试

**目的**: 验证拖拽文件夹上传功能

**步骤**:
1. 准备一个包含多个文件的文件夹（包括大小文件）
2. 将文件夹拖拽到上传区域
3. 点击"开始上传"

**预期结果**:
- 文件夹中所有文件都添加到上传队列
- 显示相对路径（例如 "folder/subfolder/file.txt"）
- 上传后保持目录结构
- 大文件自动使用分片上传

### 6. 网络中断恢复测试（可选）

**目的**: 验证网络波动时的稳定性

**步骤**:
1. 开始上传一个大文件
2. 在上传过程中暂时断开网络（例如关闭 WiFi）
3. 几秒后恢复网络

**预期结果**:
- 失败的分片会自动重试
- 上传最终能够完成
- 或者显示明确的错误信息

## 性能指标

### 上传速度对比

**测试文件**: 20MB 视频文件

**普通上传**:
- 预计时间: 根据网络速度，可能 30-60 秒
- 特点: 一次性上传，进度可能不够平滑

**分片上传**:
- 预计时间: 相似或更快（并发上传）
- 特点: 进度平滑，可取消，更稳定

### 并发性能

**测试**: 同时上传 5 个 10MB 文件

**预期**:
- 分片上传最多 3 个分片并发
- 总体上传时间应该比顺序上传快
- 浏览器不会卡顿

## 常见问题排查

### 1. 上传失败

**可能原因**:
- 空间不足
- 网络连接问题
- 服务器权限问题
- 文件名包含特殊字符

**排查方法**:
- 检查浏览器控制台错误信息
- 检查后端日志
- 验证剩余空间是否充足

### 2. 进度卡住不动

**可能原因**:
- 网络速度慢
- 服务器处理慢
- 后端超时

**排查方法**:
- 检查网络连接
- 查看后端日志
- 等待或取消重试

### 3. 文件上传后找不到

**可能原因**:
- 路径错误
- 权限问题
- 上传到错误的目录

**排查方法**:
- 检查当前路径
- 刷新文件列表
- 检查后端日志确认上传位置

## 浏览器兼容性

**支持的浏览器**:
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

**不支持的功能**:
- IE 11 及以下（不支持 FormData、File API）

## 后端日志检查

**查看上传日志**:
```bash
# 查看后端日志
tail -f backend/logs/upload.log

# 查看临时文件
ls -la backend/temp/uploads/
```

**清理临时文件**:
```bash
# 手动清理超过 24 小时的临时文件
find backend/temp/uploads/ -type d -mtime +1 -exec rm -rf {} \;
```

## 测试清单

- [ ] 小文件上传（< 5MB）
- [ ] 大文件上传（> 5MB）
- [ ] 取消上传
- [ ] 多文件并发上传
- [ ] 文件夹上传
- [ ] 进度显示正确
- [ ] 错误提示清晰
- [ ] 文件列表刷新
- [ ] 空间计算正确
- [ ] 临时文件清理

## 性能优化建议

1. **调整分片大小**: 根据网络速度调整 `CHUNK_SIZE`
2. **调整并发数**: 根据服务器性能调整 `MAX_CONCURRENT`
3. **调整阈值**: 根据实际情况调整 `shouldUseChunkedUpload` 的阈值
4. **启用压缩**: 考虑在上传前压缩文件

## 总结

分片上传功能已完整实现，包括：
- ✅ 自动判断使用分片或普通上传
- ✅ 并发上传提升速度
- ✅ 实时进度显示
- ✅ 取消上传功能
- ✅ 自动清理临时文件
- ✅ 错误处理和重试

按照本指南进行测试，确保所有功能正常工作。
