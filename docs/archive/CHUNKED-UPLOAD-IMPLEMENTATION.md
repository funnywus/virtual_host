# 分片上传功能实现说明

## 概述
为了优化大文件上传速度和稳定性，实现了分片上传功能。当文件大小超过 5MB 时，自动使用分片上传；小于 5MB 的文件继续使用普通上传。

## 实现方案

### 1. 后端实现 (`backend/routes/upload-chunked.js`)

#### 核心功能
- **初始化上传** (`POST /init-chunk`): 创建上传会话，生成唯一 uploadId
- **上传分片** (`POST /upload-chunk`): 接收并保存单个分片
- **合并分片** (`POST /merge-chunks`): 合并所有分片并上传到远程服务器
- **取消上传** (`POST /cancel-upload`): 清理临时文件
- **自动清理**: 每小时清理超过 24 小时的临时文件

#### 技术细节
- 分片大小: 2MB
- 临时目录: `backend/temp/uploads/`
- 使用 multer 处理文件上传
- 使用 fs.createWriteStream 合并分片
- 通过 SSH/SFTP 上传到远程服务器

### 2. 前端实现

#### 工具类 (`frontend/src/utils/chunked-upload.js`)

```javascript
class ChunkedUploader {
  // 初始化上传会话
  async init()
  
  // 上传单个分片
  async uploadChunk(chunkIndex)
  
  // 并发上传所有分片（最大并发 3 个）
  async uploadAllChunks()
  
  // 合并分片
  async merge()
  
  // 开始上传（完整流程）
  async start()
  
  // 取消上传
  async abort()
}
```

#### 集成到 Upload.vue

**判断逻辑**:
```javascript
// 大于 5MB 使用分片上传
if (shouldUseChunkedUpload(item.file.size)) {
  // 使用 ChunkedUploader
} else {
  // 使用普通 XMLHttpRequest 上传
}
```

**进度显示**:
- 分片上传: 0-90% 显示上传进度，90-100% 显示"合并中..."
- 普通上传: 0-50% 显示上传进度，50-100% 显示"处理中..."

**取消功能**:
- 上传中的文件显示取消按钮
- 点击取消会调用 `uploader.abort()` 并清理临时文件

## 优势

### 1. 性能提升
- **并发上传**: 最多 3 个分片同时上传，充分利用带宽
- **断点续传**: 失败的分片自动重试
- **减少内存占用**: 分片处理，不需要一次性加载整个文件

### 2. 稳定性提升
- **容错能力**: 单个分片失败不影响整体上传
- **超时处理**: 小分片上传更不容易超时
- **网络波动**: 失败分片自动重试

### 3. 用户体验
- **实时进度**: 精确显示上传进度
- **可取消**: 随时取消上传并清理资源
- **状态提示**: 清晰显示"上传中"、"合并中"等状态

## 使用场景

### 自动判断
- 文件 ≤ 5MB: 普通上传（快速简单）
- 文件 > 5MB: 分片上传（稳定可靠）

### 适用文件类型
- 视频文件
- 大型压缩包
- 高分辨率图片
- 大型数据文件

## 配置参数

### 可调整参数
```javascript
// 分片大小（默认 2MB）
const CHUNK_SIZE = 2 * 1024 * 1024;

// 最大并发数（默认 3）
const MAX_CONCURRENT = 3;

// 使用分片上传的阈值（默认 5MB）
function shouldUseChunkedUpload(fileSize) {
  return fileSize > 5 * 1024 * 1024;
}
```

## 测试建议

1. **小文件测试** (< 5MB): 验证普通上传正常工作
2. **大文件测试** (> 5MB): 验证分片上传和进度显示
3. **网络中断测试**: 验证失败重试机制
4. **取消上传测试**: 验证资源清理
5. **并发上传测试**: 同时上传多个大文件

## 注意事项

1. **临时文件清理**: 后端每小时自动清理超过 24 小时的临时文件
2. **磁盘空间**: 确保服务器有足够空间存储临时分片
3. **超时设置**: 后端已设置 10 分钟超时，支持大文件上传
4. **并发限制**: 前端限制最多 3 个分片并发，避免占用过多资源

## 未来优化方向

1. **断点续传**: 保存上传进度，刷新页面后可继续上传
2. **秒传功能**: 通过文件 hash 判断是否已存在
3. **压缩上传**: 上传前压缩文件，减少传输量
4. **P2P 传输**: 使用 WebRTC 实现点对点传输
