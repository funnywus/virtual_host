# 到期时间功能说明

## 功能概述
为域名管理和服务器管理添加了到期时间字段，方便管理员跟踪资源的到期情况。

## 功能特性

### 1. 域名管理 (Domains.vue)
- **表格显示**
  - 添加"到期时间"列，显示格式化的日期时间
  - 根据剩余天数显示不同颜色：
    - 🔴 红色：已过期
    - 🟠 橙色：7天内到期
    - 🟡 黄色：30天内到期
    - 🟢 绿色：正常（30天以上）
    - ⚪ 灰色：未设置

- **编辑对话框**
  - 添加"到期时间"日期时间选择器
  - 支持清空（表示永久有效）
  - 格式：YYYY-MM-DD HH:mm:ss

### 2. 服务器管理 (Servers.vue)
- **表格显示**
  - 添加"到期时间"列，显示格式化的日期时间
  - 颜色规则与域名管理相同

- **编辑对话框**
  - 添加"到期时间"日期时间选择器
  - 支持清空（表示永久有效）
  - 格式：YYYY-MM-DD HH:mm:ss

## 时间格式化

### 显示格式
```
2024-12-31 23:59:59
```

### 颜色规则
```javascript
已过期（< 0天）    → #FF3B30 (红色)
即将到期（≤ 7天）  → #FF9500 (橙色)
临近到期（≤ 30天） → #FFCC00 (黄色)
正常（> 30天）     → #34C759 (绿色)
未设置            → #909399 (灰色)
```

## 使用方法

### 添加/编辑域名
1. 点击"添加域名"或"编辑"按钮
2. 填写域名信息
3. 在"到期时间"字段选择日期时间
4. 点击"确定"保存

### 添加/编辑服务器
1. 点击"添加服务器"或"编辑"按钮
2. 填写服务器信息
3. 在"到期时间"字段选择日期时间
4. 点击"确定"保存

### 查看到期状态
- 在列表中直接查看"到期时间"列
- 根据颜色快速识别到期状态
- 红色和橙色表示需要及时处理

## 后端数据库

需要在数据库表中添加 `expire_at` 字段：

### domains 表
```sql
ALTER TABLE domains ADD COLUMN expire_at DATETIME DEFAULT NULL;
```

### servers 表
```sql
ALTER TABLE servers ADD COLUMN expire_at DATETIME DEFAULT NULL;
```

## API 接口

### 域名接口
- `POST /dns/domains` - 创建域名（包含 expire_at）
- `PUT /dns/domains/:id` - 更新域名（包含 expire_at）

### 服务器接口
- `POST /servers` - 创建服务器（包含 expire_at）
- `PUT /servers/:id` - 更新服务器（包含 expire_at）

## 注意事项

1. **时区处理**
   - 前端使用本地时区显示
   - 后端存储使用 UTC 时间或服务器时区

2. **空值处理**
   - expire_at 为 null 表示永久有效
   - 显示为"-"或"永久"

3. **提醒机制**
   - 建议配合定时任务实现到期提醒
   - 可以在首页添加到期提醒面板

4. **权限控制**
   - 只有管理员可以设置到期时间
   - 普通用户只能查看

## 未来扩展

1. **到期提醒**
   - 邮件提醒
   - 站内消息
   - 微信/钉钉通知

2. **自动续期**
   - 集成支付系统
   - 自动续期功能

3. **批量操作**
   - 批量设置到期时间
   - 批量续期

4. **统计报表**
   - 到期资源统计
   - 续费趋势分析
