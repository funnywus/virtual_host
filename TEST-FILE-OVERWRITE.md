# 文件覆盖功能测试指南

## 快速测试

### 测试 1: 单个文件覆盖

1. 上传一个文件（如 test.txt）
2. 等待上传完成
3. 再次上传同名文件 test.txt
4. 应该看到提示："文件 'test.txt' 已存在，是否覆盖？"
5. 点击"覆盖"，文件应该被添加到队列
6. 点击"跳过"，文件不应该被添加

**预期结果：**
- ✅ 显示覆盖确认对话框
- ✅ 点击"覆盖"后文件添加到队列
- ✅ 点击"跳过"后显示提示："已跳过该文件"

### 测试 2: 批量文件覆盖

1. 创建测试文件夹，包含 5 个文件：
   - file1.txt
   - file2.txt
   - file3.txt
   - file4.txt
   - file5.txt

2. 上传这 5 个文件
3. 等待上传完成
4. 再次上传这 5 个文件
5. 应该看到提示："有 5 个文件已存在，如何处理？"

**测试场景 A - 全部覆盖：**
- 点击"全部覆盖"
- 所有 5 个文件应该添加到队列
- 显示提示："已添加 5 个文件（将覆盖已存在的文件）"

**测试场景 B - 全部跳过：**
- 点击"全部跳过"
- 没有文件添加到队列
- 显示提示："已跳过所有文件"

**测试场景 C - 关闭对话框：**
- 点击右上角 X 关闭
- 没有文件添加到队列
- 显示提示："已取消添加文件"

### 测试 3: 部分文件冲突

1. 上传 3 个文件：a.txt, b.txt, c.txt
2. 等待上传完成
3. 准备 5 个文件：a.txt, b.txt, c.txt, d.txt, e.txt
4. 上传这 5 个文件
5. 应该看到提示："有 3 个文件已存在，如何处理？"

**测试场景 A - 全部覆盖：**
- 点击"全部覆盖"
- 所有 5 个文件添加到队列
- 显示提示："已添加 5 个文件（将覆盖已存在的文件）"

**测试场景 B - 全部跳过：**
- 点击"全部跳过"
- 只有 d.txt 和 e.txt 添加到队列
- 显示提示："已跳过 3 个文件，添加了 2 个文件"

### 测试 4: 拖拽上传

1. 上传 index.html
2. 等待上传完成
3. 拖拽 index.html 到上传区域
4. 应该看到覆盖确认对话框

**预期结果：**
- ✅ 拖拽触发文件冲突检测
- ✅ 显示覆盖确认对话框
- ✅ 功能与点击上传一致

### 测试 5: 文件夹上传

1. 创建文件夹结构：
   ```
   website/
   ├── index.html
   ├── style.css
   └── images/
       └── logo.png
   ```

2. 上传整个 website 文件夹
3. 等待上传完成
4. 再次上传 website 文件夹
5. 应该检测到根目录的文件冲突

**预期结果：**
- ✅ 检测到 index.html 和 style.css 冲突
- ✅ 显示批量覆盖对话框
- ✅ 子目录文件（images/logo.png）不触发冲突

## 边界测试

### 边界 1: 空文件名

1. 尝试上传文件名为空的文件
2. 系统应该正常处理

### 边界 2: 特殊字符文件名

1. 上传包含特殊字符的文件：
   - `test (1).txt`
   - `文件-测试.txt`
   - `file@#$.txt`

2. 再次上传同名文件
3. 应该正确检测冲突

### 边界 3: 大量文件

1. 准备 100 个文件
2. 上传这 100 个文件
3. 再次上传这 100 个文件
4. 应该显示："有 100 个文件已存在，如何处理？"

**性能要求：**
- ✅ 检测时间 < 500ms
- ✅ 对话框响应流畅
- ✅ 不卡顿

### 边界 4: 不同目录

1. 在根目录上传 test.txt
2. 进入子目录 /images
3. 上传 test.txt
4. 不应该触发冲突（不同目录）

**预期结果：**
- ✅ 不同目录的同名文件不冲突
- ✅ 文件正常添加到队列

## 集成测试

### 集成 1: 与普通上传配合

1. 上传文件触发覆盖确认
2. 选择"覆盖"
3. 点击"开始上传"
4. 文件应该正常上传并覆盖

### 集成 2: 与分片上传配合

1. 上传大文件（> 5MB）触发覆盖确认
2. 选择"覆盖"
3. 使用分片上传
4. 应该正常覆盖文件

### 集成 3: 与 WebSocket 直传配合

1. 启用直传模式
2. 上传文件触发覆盖确认
3. 选择"覆盖"
4. 使用 WebSocket 直传
5. 应该正常覆盖文件

## 用户体验测试

### UX 1: 对话框文案

- ✅ 文案清晰易懂
- ✅ 按钮文字明确
- ✅ 提示信息友好

### UX 2: 操作流畅性

- ✅ 对话框弹出流畅
- ✅ 按钮响应及时
- ✅ 没有卡顿

### UX 3: 错误处理

- ✅ 异常情况有提示
- ✅ 不会导致页面崩溃
- ✅ 可以正常恢复

## 回归测试

确保新功能不影响现有功能：

- ✅ 普通上传仍然正常
- ✅ 分片上传仍然正常
- ✅ WebSocket 直传仍然正常
- ✅ 文件列表显示正常
- ✅ 文件删除功能正常
- ✅ 文件重命名功能正常

## 测试检查清单

- [ ] 单个文件覆盖确认
- [ ] 批量文件全部覆盖
- [ ] 批量文件全部跳过
- [ ] 批量文件取消操作
- [ ] 部分文件冲突处理
- [ ] 拖拽上传冲突检测
- [ ] 文件夹上传冲突检测
- [ ] 特殊字符文件名
- [ ] 大量文件性能
- [ ] 不同目录同名文件
- [ ] 与普通上传集成
- [ ] 与分片上传集成
- [ ] 与 WebSocket 直传集成
- [ ] 用户体验流畅性
- [ ] 现有功能不受影响

## 已知限制

1. **只检测当前目录**
   - 子目录中的文件不会触发冲突检测
   - 这是设计决策，避免过于复杂

2. **前端检测**
   - 检测在前端进行
   - 如果文件列表未刷新，可能检测不准确
   - 建议上传前刷新文件列表

3. **性能考虑**
   - 大量文件（> 1000）时检测可能较慢
   - 建议分批上传

## 故障排查

### 问题 1: 没有显示覆盖确认

**可能原因：**
- 文件列表未刷新
- 文件名不匹配（大小写）

**解决方案：**
- 刷新文件列表
- 检查文件名是否完全一致

### 问题 2: 对话框显示错误

**可能原因：**
- Element Plus 版本问题
- 浏览器兼容性

**解决方案：**
- 更新 Element Plus
- 使用现代浏览器

### 问题 3: 批量处理不正确

**可能原因：**
- 文件路径解析错误
- 逻辑判断问题

**解决方案：**
- 查看浏览器控制台错误
- 检查文件路径是否正确

## 测试报告模板

```
测试日期：2026-04-06
测试人员：[姓名]
浏览器：Chrome 120 / Firefox 120 / Safari 17

测试结果：
✅ 单个文件覆盖：通过
✅ 批量文件覆盖：通过
✅ 部分文件冲突：通过
✅ 拖拽上传：通过
✅ 文件夹上传：通过
✅ 性能测试：通过
✅ 集成测试：通过
✅ 回归测试：通过

发现问题：
1. [问题描述]
2. [问题描述]

总体评价：
功能正常，用户体验良好，可以发布。
```
